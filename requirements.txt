# scripts/odt2md.py uses only the Python standard library — no third-party
# packages are required to run it. This file is kept as the conventional
# place to add one if the tooling ever grows a dependency.
