# Legacy ODT source tree and old conversion scripts — kept locally, not
# versioned here. `original/` is the converted, versioned output.
/SOF/

# Generated website (scripts/md2html.py); the default output of
# `md2html.py original` plus the names a build is likely to be sent to.
/original-html/
/site/
/sof-catalog/
/sof-catalog-new/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Editor / OS cruft
.DS_Store
*.swp
